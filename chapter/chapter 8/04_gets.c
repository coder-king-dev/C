#include <stdio.h>

int main(){
    // if you want to enter spaces in your str 
    char str[50];
    printf("Enter Text: ");
    gets(str); // use fgets ask **GPT**
    puts(str); // print next line without \n and did not use %s
    printf("Hello"); // you can print any word after string without \n

    return 0;
}